# 1. 虚拟环境 (Virtual Environment)
# 这是一个很大的文件夹，里面全是库文件，不需要上传
.venv/

# 2. Python 缓存文件 (Python Cache)
# 运行代码时自动生成的，不需要上传
__pycache__/
*.pyc

# 3. 数据集 (Datasets)
# 数据通常很大，且可以重新下载
data/

# 4. 模型权重 (Model Checkpoints)
# 训练出来的二进制大文件
checkpoints/
*.pth

# 5. 运行结果 (Results)
# 除非你想展示具体的输出图表，否则通常也会忽略
results/

# 6. IDE 配置文件 (IDE Configs)
# 个人的编辑器配置，通常不上传
.vscode/
.claude/
.idea/

# 7. 系统文件 (OS Files)
# Mac 系统自动生成的垃圾文件
.DS_Store